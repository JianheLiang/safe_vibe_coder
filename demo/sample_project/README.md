# Demo Sample Project

This directory intentionally contains secrets and destructive SQL so the VibeGuard prototype can demonstrate:

- redaction before file content is passed to an LLM
- risky command review before shell or SQL is proposed
- learned-rule accumulation with `INTERNAL_TOKEN`
